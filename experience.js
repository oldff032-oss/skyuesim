const xpToken=localStorage.getItem('signal_session_token');
if(!xpToken)location.href='login.html';
const xpEsc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
async function xpFetch(path,options={}){const response=await fetch(`${API_URL}${path}`,{cache:'no-store',...options,headers:{'Content-Type':'application/json','x-session-token':xpToken,...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(response.status===401){localStorage.removeItem('signal_session_token');location.href='login.html';throw Error('Сесія завершена');}if(!response.ok)throw Error(data.error||'Не вдалося виконати дію');return data;}
function xpToast(message){let toast=document.querySelector('.xp-toast');if(!toast){toast=document.createElement('div');toast.className='xp-toast';document.body.append(toast);}toast.textContent=message;toast.classList.add('show');clearTimeout(window.xpToastTimer);window.xpToastTimer=setTimeout(()=>toast.classList.remove('show'),2800);}
function xpNav(active='universe'){return `<nav class="xp-nav" aria-label="Головна навігація"><a href="dashboard.html" aria-label="Головна"><img src="nav-home-v2.png" alt=""></a><a href="signal-universe.html" class="${active==='universe'?'active':''}" aria-label="Signal Space">✦</a><a href="usage.html" class="${active==='usage'?'active':''}" aria-label="Витрати"><img src="nav-usage-v2.png" alt=""></a><a href="profile.html" aria-label="Профіль"><img src="nav-profile-v2.png" alt=""></a></nav>`;}
function xpDate(value){return value?new Date(value).toLocaleDateString('uk-UA',{day:'numeric',month:'short',year:'numeric'}):'Без дати';}
